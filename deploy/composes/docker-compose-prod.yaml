services:
  tasks:
    container_name: tasks
    build:
      context: ../../tasks
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped

  client:
    container_name: frontend
    build:
      context: ../../client
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    restart: unless-stopped
